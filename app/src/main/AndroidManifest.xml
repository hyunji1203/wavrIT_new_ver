<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test4">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Test4">
        <activity android:name=".mypage.MypageActivity"></activity>
        <activity android:name=".Auth.SplachActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Auth.LoginActivity" />
        <activity android:name=".Auth.JoinActivity" />
        <activity android:name=".Auth.IntroActivity" />
        <activity android:name=".community.BoardWriteActivity" />
        <activity android:name=".search.Search_Result_Activity" />
        <activity android:name=".search.SearchActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>