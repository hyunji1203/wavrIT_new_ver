<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test4">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Test4">
        <activity android:name=".board.BoardWriteActivity" />
        <activity android:name=".board.BoardInsideActivity" />
        <activity android:name=".board.BoardEditActivity" />
        <activity android:name=".Auth.Sound_textsize_Activity" />
        <activity android:name=".Auth.NicknameActivity" />
        <activity android:name=".Auth.TypeActivity" />
        <activity android:name=".Auth.WhereActivity" />
        <activity android:name=".Auth.WaitActivity" />
        <activity android:name=".mypage.FilterActivity" />
        <activity android:name=".mypage.MypageActivity" />
        <activity android:name=".Auth.SplachActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Auth.LoginActivity" />
        <activity android:name=".Auth.JoinActivity" />
        <activity android:name=".Auth.IntroActivity" />
        <activity android:name=".search.Search_Result_Activity" />
        <activity android:name=".search.SearchActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>